# Copy to .env and fill in. Never commit .env.
# Vars prefixed with PUBLIC_ are exposed to client-side code.

# Canonical site URL, used for absolute links / sitemap / OG tags.
PUBLIC_SITE_URL=https://example.com
